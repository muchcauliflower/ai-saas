import { Button } from "@/components/ui/button";

export default function DashboardPage() {
  return (
    <div>
      <h1>Welcome to the Dashboard (Protected)</h1>
      <Button variant="destructive">Click Me</Button>
    </div>
  );
}
